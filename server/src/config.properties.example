# DATABASE CONFIGURATION
rlDB=jdbc:mysql://localhost:3306/miaam
usernameDB=root
passwordDB=
serverDB=localhost
portDB=3306
nameDB=miaam

# SERVICE API (Service Central)
host=localhost
port=8080

# RMI
rmi_host=localhost
rmi_registry_port=1234
rmi_service_name=proxy

# EXTERNAL API
api.url=https://carto.g-ny.org/data/cifs/cifs_waze_v2.json

# SSL
keystore_path=serveur_keystore.jks
keystore_password=monpassword